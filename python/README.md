# pstd Python wrapper

This package is an orchestration boundary for the Rust `pstd` binary.

M1 does not make Python responsible for PST parsing or archive writing.

Run after packaging is added:

```text
python -m pstd --help
```
