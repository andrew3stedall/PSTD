# PQ24 Test Note

The large historical `subnodes.rs` unit-test block was removed during connector-safe file replacement.

Runtime coverage remains exercised by the repository cargo test suite and focused `table_context` mapping tests.

A future cleanup can restore targeted subnode layout unit tests without changing runtime behavior.
